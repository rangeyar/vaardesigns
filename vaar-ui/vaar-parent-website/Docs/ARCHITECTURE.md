# Deployment Architecture & Flow

## 🏗️ Your Current AWS Infrastructure (Terraform)

```
┌─────────────────────────────────────────────────────────────┐
│                     AWS Infrastructure                       │
│                    (Managed by Terraform)                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐ │
│  │   Route 53   │───▶│  CloudFront  │───▶│  S3 Bucket   │ │
│  │   (DNS)      │    │   (CDN)      │    │  (Storage)   │ │
│  └──────────────┘    └──────────────┘    └──────────────┘ │
│         │                    │                              │
│         │                    │                              │
│  ┌──────▼──────┐            │                              │
│  │     ACM     │            │                              │
│  │ (SSL Cert)  │────────────┘                              │
│  └─────────────┘                                            │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 GitHub Actions Deployment Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                         DEVELOPER                               │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            │ git push
                            ▼
┌─────────────────────────────────────────────────────────────────┐
│                         GITHUB                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │              GitHub Actions Workflow                    │    │
│  ├────────────────────────────────────────────────────────┤    │
│  │  1. Checkout Code                                      │    │
│  │  2. Setup Node.js 20                                   │    │
│  │  3. Install Dependencies (npm ci)                      │    │
│  │  4. Run Linter (ESLint)                                │    │
│  │  5. Build React App (npm run build) → dist/            │    │
│  └──────────────────────────┬─────────────────────────────┘    │
└─────────────────────────────┼──────────────────────────────────┘
                              │
                              │ AWS CLI
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                            AWS                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  6. Upload files to S3 (aws s3 sync dist/ ...)         │    │
│  │     • Assets cached for 1 year                          │    │
│  │     • index.html never cached                           │    │
│  └──────────────────────────┬─────────────────────────────┘    │
│                              │                                  │
│  ┌──────────────────────────▼─────────────────────────────┐    │
│  │  7. Invalidate CloudFront Cache (/*) ←────┐            │    │
│  │     • Forces CDN to fetch new files        │            │    │
│  │     • Takes 5-10 minutes                   │            │    │
│  └────────────────────────────────────────────┘            │    │
└─────────────────────────────────────────────────────────────────┘
                              │
                              │ Cache invalidation complete
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                      END USERS                                  │
│                                                                 │
│  https://your-domain.com → Updated Website! 🎉                 │
└─────────────────────────────────────────────────────────────────┘
```

## 🔐 Authentication Flow

```
┌──────────────┐
│   GitHub     │
│   Secrets    │
│              │
│  • AWS_ACCESS_KEY_ID              ┐
│  • AWS_SECRET_ACCESS_KEY          │
│  • AWS_REGION                     │  Passed to
│  • AWS_S3_BUCKET                  │  GitHub Actions
│  • AWS_CLOUDFRONT_DISTRIBUTION_ID │
└────────┬─────┘                    │
         │                          │
         │                          │
         ▼                          ┘
┌──────────────────────────┐
│   GitHub Actions         │
│   (Worker VM)            │
│                          │
│  Uses credentials to     │
│  authenticate with AWS   │
└────────┬─────────────────┘
         │
         │ AWS CLI commands
         ▼
┌──────────────────────────┐
│   IAM User               │
│   github-actions-deploy  │
│                          │
│   Permissions:           │
│   • S3: Put/Get/Delete   │
│   • CloudFront: Invalidate│
└────────┬─────────────────┘
         │
         │ Authorized
         ▼
┌──────────────────────────┐
│   AWS Resources          │
│   • S3 Bucket            │
│   • CloudFront Dist      │
└──────────────────────────┘
```

## 📦 File Types & Caching Strategy

```
┌─────────────────────────────────────────────────────────────┐
│                    dist/ (Build Output)                     │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  index.html                     ← No cache (always fresh)   │
│  Cache-Control: max-age=0                                   │
│                                                              │
│  assets/                                                     │
│  ├── main.abc123.js            ← Cache 1 year (hashed)      │
│  ├── main.abc123.css           ← Cache 1 year (hashed)      │
│  ├── logo.xyz789.png           ← Cache 1 year (hashed)      │
│  └── ...                                                     │
│  Cache-Control: max-age=31536000, immutable                 │
│                                                              │
│  robots.txt, sitemap.xml        ← No long-term cache        │
│                                                              │
└─────────────────────────────────────────────────────────────┘

Why this strategy?
• index.html: Always fetched fresh (contains references to new assets)
• Assets: Vite adds hash to filenames, safe to cache forever
• Result: Fast loading + instant updates when you deploy
```

## 🔄 Deployment Timeline

```
Time: 0:00    Developer pushes code to GitHub
      ↓
      0:10    GitHub Actions triggered
      ↓
      0:30    Dependencies installed
      ↓
      1:00    Build completed
      ↓
      1:30    Files uploaded to S3
      ↓
      1:45    CloudFront invalidation started
      ↓
      2:00    GitHub Actions workflow complete ✅
      ↓
      ...     CloudFront propagating changes...
      ↓
      8:00    Website fully updated worldwide 🌍

Total: ~8-10 minutes from push to live
```

## 🌍 Multi-Region Distribution (CloudFront)

```
                    ┌──────────────────┐
                    │   CloudFront     │
                    │   (Global CDN)   │
                    └────────┬─────────┘
                             │
            ┌────────────────┼────────────────┐
            │                │                │
            ▼                ▼                ▼
    ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
    │ Edge Location│  │ Edge Location│  │ Edge Location│
    │  US-East     │  │  EU-West     │  │  Asia-Pacific│
    └──────────────┘  └──────────────┘  └──────────────┘
            │                │                │
            ▼                ▼                ▼
         [Users]          [Users]          [Users]

• Content cached at edge locations
• Faster load times globally
• Reduced S3 data transfer costs
• HTTPS/SSL handled by CloudFront
```

## 🔀 Branch Strategy Examples

### Option 1: Single Environment

```
developer branch ──push──▶ GitHub Actions ──deploy──▶ Production
main branch ─────push──▶ GitHub Actions ──deploy──▶ Production
```

### Option 2: Multi-Environment (Recommended)

```
developer branch ──push──▶ GitHub Actions ──deploy──▶ Staging
                                                       (staging.domain.com)
      │
      │ Pull Request + Review
      ▼
main branch ─────push──▶ GitHub Actions ──deploy──▶ Production
                                                      (www.domain.com)
```

### Option 3: Feature Branches

```
feature/* branches ──push──▶ Run tests only (no deploy)
      │
      │ Pull Request
      ▼
developer branch ──merge──▶ Deploy to Staging
      │
      │ Testing & QA
      ▼
main branch ─────merge──▶ Deploy to Production
```

## 📊 Cost Breakdown (Typical Small Site)

```
Service         Free Tier              Typical Cost/Month
────────────────────────────────────────────────────────
S3              5GB storage            $0.10 - $1.00
                20,000 GET requests
                2,000 PUT requests

CloudFront      1TB data transfer      $0.00 (within free tier)
                10,000,000 requests    for small sites

Route 53        $0.50/hosted zone      $0.50
                1M queries free

ACM             Free SSL certificates  $0.00

GitHub Actions  2,000 minutes/month    $0.00 (public repos)
                (free for public)      unlimited

IAM             Free                   $0.00
────────────────────────────────────────────────────────
Total                                  $0.60 - $1.50/month
```

## 🎯 Summary

**What you have:**

- ✅ Terraform-managed AWS infrastructure
- ✅ S3 for static hosting
- ✅ CloudFront for global CDN
- ✅ Route 53 for DNS
- ✅ ACM for SSL/HTTPS

**What we added:**

- ✅ GitHub Actions workflow
- ✅ Automated build process
- ✅ Automated deployment to S3
- ✅ Automatic CloudFront cache invalidation
- ✅ IAM user with minimal permissions

**Result:**

- 🚀 Push code → Auto-deploy
- ⚡ Fast global delivery
- 🔒 Secure HTTPS
- 💰 Very low cost
- 🎯 Zero-downtime deployments
